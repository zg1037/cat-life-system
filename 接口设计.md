# 猫咪生活记录（CatLife）后端接口设计文档

> **版本：V1.0**
>
> **项目名称：CatLife（猫咪生活记录）**
>
> **更新时间：2026-06-29**

---

# 1. 项目简介

CatLife 是一款用于记录猫咪日常生活的 H5 应用，帮助用户记录猫咪的喂养、睡眠、排泄、玩耍、心情、健康、体重及成长日记，并通过统计分析展示猫咪成长情况。

后端采用 RESTful API 设计，使用 JWT 进行身份认证，Redis 进行缓存优化，文件采用服务器本地目录存储。

---

# 2. 技术架构

## 技术选型

| 技术              | 说明      |
| --------------- | ------- |
| Spring Boot 3   | Web框架   |
| Spring Security | 登录认证    |
| JWT             | Token认证 |
| MyBatis Plus    | ORM框架   |
| MySQL 8         | 数据存储    |
| Redis           | 缓存      |
| Local Upload    | 本地文件存储  |
| Maven           | 项目管理    |

---

## 系统架构

```
Vue3(H5)

      │

      ▼

Spring Boot API

      │

 ┌────┴───────────────┐

 ▼                    ▼

MySQL              Redis

      │

      ▼

upload/
```

职责说明：

* **MySQL**：业务数据存储
* **Redis**：热点数据缓存
* **upload/**：图片、视频等文件存储

---

# 3. 项目结构

```
cat-life
├── common
│   ├── config
│   ├── constant
│   ├── exception
│   ├── interceptor
│   ├── result
│   └── utils
│
├── controller
├── service
├── mapper
├── entity
├── dto
├── vo
├── statistics
├── upload
└── resources
```

---

# 4. 接口规范

## 请求方式

采用 RESTful 风格。

| 方法     | 说明 |
| ------ | -- |
| GET    | 查询 |
| POST   | 新增 |
| PUT    | 修改 |
| DELETE | 删除 |

---

## 请求头

```
Authorization: Bearer JWT_TOKEN
Content-Type: application/json
```

---

## 统一返回格式

```json
{
    "code":200,
    "message":"success",
    "data":{}
}
```

---

## 分页规范

```
?page=1
&pageSize=10
```

返回：

```json
{
    "code":200,
    "message":"success",
    "data":{
        "total":100,
        "records":[]
    }
}
```

---

# 5. 用户模块

## 用户注册

### POST

```
/api/auth/register
```

请求参数

| 参数       | 类型     | 是否必填 | 说明  |
| -------- | ------ | ---- | --- |
| username | String | √    | 用户名 |
| password | String | √    | 密码  |
| nickname | String | √    | 昵称  |

---

## 用户登录

### POST

```
/api/auth/login
```

请求参数

| 参数       | 类型     |
| -------- | ------ |
| username | String |
| password | String |

返回：

```json
{
    "code":200,
    "message":"success",
    "data":{
        "token":"jwt_token",
        "userInfo":{
            "nickname":"用户昵称",
            "avatar":"https://...png"
        }
    }
}
```

---

## 退出登录

### POST

```
/api/auth/logout
```

---

## 获取个人信息

### GET

```
/api/user/profile
```

---

## 修改个人资料

### PUT

```
/api/user/profile
```

---

## 修改密码

### PUT

```
/api/user/password
```

---

# 6. 猫咪模块

## 获取猫咪列表

### GET

```
/api/cats
```

---

## 新增猫咪

### POST

```
/api/cats
```

请求参数：

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| name | String | √ | 猫咪名称 |
| avatar | String | | 头像 URL |
| breed | String | | 品种 |
| gender | String | | male / female |
| birthDate | String | | 出生日期 YYYY-MM-DD |
| furColor | String | | 毛色 |
| isNeutered | Boolean | | 是否绝育 |
| bio | String | | 简介 |

---

## 修改猫咪

### PUT

```
/api/cats/{id}
```

请求参数与新增一致。

---

## 获取猫咪详情

### GET

```
/api/cats/{id}
```

查询流程：

```
Redis

↓

存在

↓

返回

↓

不存在

↓

MySQL

↓

写入Redis

↓

返回
```

Redis：

```
cat:{catId}
```

---

## 切换当前猫咪

### PUT

```
/api/cats/switch/{id}
```

说明：记录用户当前选中的猫咪 ID，首页及相关页面按此 ID 筛选数据。

---

## 删除猫咪

### DELETE

```
/api/cats/{id}
```

---

## 获取首页统计

### GET

```
/api/cats/{id}/today-stats
```

返回当前猫咪今日统计数据：

```json
{
    "code":200,
    "message":"success",
    "data":{
        "mealCount": 2,
        "waterCount": 1,
        "poopCount": 1,
        "sleepHours": 12.5,
        "playCount": 1
    }
}
```

---

## 删除猫咪

### DELETE

```
/api/cats/{id}
```

---

# 日常记录模块

统一接口：

```
/api/records
```

记录类型（与前端 Tab 一一对应）：

| 类型 | 说明 |
|------|------|
| `meal` | 饮食喝水（合并） |
| `poop` | 排泄 |
| `sleep` | 睡眠 |
| `play` | 玩耍 |
| `mood` | 状态 |

> 前端 "饮食喝水" 为一个 Tab，包含食物名称、重量g、是否吃完、饮水量ml 四个字段。

---

## 新增记录

### POST

```
/api/records
```

请求参数（通用字段）：

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| catId | Long | √ | 猫咪 ID |
| type | String | √ | 记录类型：meal / poop / sleep / play / mood |
| recordTime | String | √ | 记录时间 "YYYY-MM-DD HH:mm" |
| note | String | | 备注 |

按 type 分不同字段：

**type = meal（饮食喝水）**

| 参数 | 类型 | 说明 |
|------|------|------|
| foodName | String | 食物名称 |
| weight | BigDecimal | 重量(g) |
| isFinished | Boolean | 是否吃完 |
| waterAmount | BigDecimal | 饮水量(ml) |

**type = poop（排泄）**

| 参数 | 类型 | 说明 |
|------|------|------|
| poopType | String | pee / poop |
| poopStatus | String | 状态：正常/软便/干硬 |

**type = sleep（睡眠）**

| 参数 | 类型 | 说明 |
|------|------|------|
| startTime | String | 开始时间 "HH:mm" |
| endTime | String | 结束时间 "HH:mm" |
| location | String | 地点 |

**type = play（玩耍）**

| 参数 | 类型 | 说明 |
|------|------|------|
| toy | String | 玩具名称 |
| playDuration | Integer | 时长(分钟) |
| playPhotos | String[] | 玩耍照片 URL 数组 |

**type = mood（状态）**

| 参数 | 类型 | 说明 |
|------|------|------|
| mood | String | energetic / lethargic |
| behavior | String | 行为表现 |

保存流程：

```
保存 MySQL
    │
    ▼
删除 today:{catId} 缓存
    │
    ▼
首页重新统计
```

---

## 查询记录列表

### GET

```
/api/records
```

支持参数：

```
?catId=1
&type=meal           // 按类型筛选
&date=2026-06-29     // 按日期筛选（精确到日）
&keyword=猫粮        // 关键词搜索（匹配 foodName / toy / note / poopStatus / behavior 等）
&page=1
&pageSize=20
```

---

## 查询记录详情

### GET

```
/api/records/{id}
```

---

## 修改记录

### PUT

```
/api/records/{id}
```

请求参数与新增一致，仅传入需要修改的字段。

---

## 删除记录

### DELETE

```
/api/records/{id}
```

---

# 日记模块

统一接口：

```
/api/diary
```

支持：图片上传、标签（字符串数组）

## 新增日记

### POST

```
/api/diary
```

请求参数：

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| catId | Long | √ | 猫咪 ID |
| date | String | √ | 日期 "YYYY-MM-DD" |
| title | String | √ | 标题（max 50） |
| content | String | √ | 正文 |
| tags | String[] | | 标签数组 |
| images | String[] | | 图片 URL 数组 |

## 查询日记列表

### GET

```
/api/diary
```

支持：

```
?catId=1
&month=2026-06     // 按月筛选
&page=1
&pageSize=20
```

返回按日期倒序排列。

---

## 查询日记详情

### GET

```
/api/diary/{id}
```

返回：

```json
{
    "code":200,
    "message":"success",
    "data":{
        "id":1,
        "catId":1,
        "date":"2026-06-28",
        "title":"今天带小橘去打疫苗了",
        "content":"小橘很勇敢...",
        "tags":["健康","疫苗"],
        "images":["https://...jpg"]
    }
}
```

## 查询日记详情

### GET

```
/api/diary/{id}
```

## 修改日记

### PUT

```
/api/diary/{id}
```

## 删除日记

### DELETE

```
/api/diary/{id}
```

---

# 健康模块

统一接口：

```
/api/health
```

健康类型：

| 类型 | 说明 |
|------|------|
| vaccine | 疫苗 |
| deworm | 驱虫 |
| medical | 医疗 |

## 新增健康记录

### POST

```
/api/health
```

请求参数：

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| catId | Long | √ | 猫咪 ID |
| type | String | √ | vaccine / deworm / medical |
| title | String | √ | 标题 |
| date | String | √ | 日期 "YYYY-MM-DD" |
| nextDate | String | | 下次日期 "YYYY-MM-DD" |
| hospital | String | | 医院名称 |
| note | String | | 备注 |

## 查询健康记录

### GET

```
/api/health
```

支持：

```
?catId=1
&startDate=2026-01-01
&endDate=2026-12-31
```

## 修改健康记录

### PUT

```
/api/health/{id}
```

## 删除健康记录

### DELETE

```
/api/health/{id}
```

---

# 体重模块

统一接口：

```
/api/weight
```

## 新增体重记录

### POST

```
/api/weight
```

请求参数：

| 参数 | 类型 | 是否必填 | 说明 |
|------|------|----------|------|
| catId | Long | √ | 猫咪 ID |
| weight | BigDecimal | √ | 体重(kg) |
| date | String | √ | 日期 "YYYY-MM-DD" |
| note | String | | 备注 |

## 查询体重记录

### GET

```
/api/weight
```

支持：

```
?catId=1
&page=1
&pageSize=20
```

## 修改体重记录

### PUT

```
/api/weight/{id}
```

## 删除体重记录

### DELETE

```
/api/weight/{id}
```

---

# 11. 文件上传模块

文件采用服务器本地目录存储。

目录：

```
upload

├── avatar

├── diary

├── health

└── temp
```

---

## 上传图片

### POST

```
/api/upload/image
```

请求：`multipart/form-data`，file 字段传文件。

返回：

```json
{
    "code":200,
    "message":"success",
    "data":{
        "url":"/upload/diary/xxx.jpg",
        "fileName":"xxx.jpg",
        "fileSize":102400
    }
}
```

---

## 删除文件

### DELETE

```
/api/upload?url=/upload/diary/xxx.jpg
```

返回：

```json
{
    "code":200,
    "message":"success",
    "data":{
        "url":"/upload/diary/20260629/abc.jpg"
    }
}
```

---

# 12. 首页统计模块

接口：

```
GET /api/statistics/today

GET /api/statistics/week

GET /api/statistics/month
```

缓存流程：

```
Redis

↓

存在

↓

直接返回

↓

不存在

↓

MySQL统计

↓

缓存5分钟

↓

返回
```

Redis：

```
today:{catId}
```

缓存内容：

```json
{
    "foodCount":2,
    "waterAmount":350,
    "sleepHours":12,
    "playMinutes":40
}
```

TTL：

```
300秒
```

---

# 13. Redis缓存设计

## 猫咪档案

```
Key

cat:{catId}
```

TTL：

```
3600秒
```

更新策略：

```
修改资料

↓

删除缓存

↓

重新查询时加载
```

---

## 首页统计

```
Key

today:{catId}
```

TTL：

```
300秒
```

---

## 登录会话（可选）

```
Key

login:{userId}
```

TTL：

```
7天
```

作用：

* 单设备登录
* Token失效
* 强制退出

---

# 14. 业务流程

```
用户登录
    │
    ▼
选择猫咪
    │
    ▼
记录日常
    │
    ├── 喂养
    │      ├── 喂食
    │      └── 饮水
    ├── 睡眠
    ├── 排泄
    ├── 玩耍
    └── 心情
    │
    ▼
更新Redis缓存
    │
    ▼
首页统计展示
```

---

# 15. 错误码

| Code | 说明     |
| ---- | ------ |
| 200  | 成功     |
| 400  | 请求参数错误 |
| 401  | 未登录    |
| 403  | 无权限    |
| 404  | 数据不存在  |
| 500  | 服务器异常  |

---

# 16. 后续扩展

* 多猫管理
* AI 成长总结
* 分享成长海报
* Excel/PDF 数据导出
* 疫苗、驱虫提醒
* 消息推送
* 第三方登录（微信、QQ、Google）
* 文件存储切换（MinIO / 阿里云 OSS）

---

# 17. 开发规范

## 命名规范

* Controller：`CatController`
* Service：`CatService`
* Mapper：`CatMapper`
* Entity：`CatInfo`
* DTO：`CatCreateDTO`
* VO：`CatDetailVO`

---

## 缓存策略

| 数据       | Redis Key        | TTL |
| -------- | ---------------- | --- |
| 猫咪信息     | `cat:{catId}`    | 1小时 |
| 首页统计     | `today:{catId}`  | 5分钟 |
| 登录会话（可选） | `login:{userId}` | 7天  |

所有涉及猫咪资料修改或日常记录新增、修改、删除的接口，都应及时删除对应缓存，保证缓存与数据库的一致性。
