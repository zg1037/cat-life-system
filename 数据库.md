# 猫咪生活记录系统（CatLife）数据库设计文档

> **版本：V1.0**
>
> **项目名称：CatLife（猫咪生活记录）**
>
> **数据库：MySQL 8**
>
> **缓存：Redis**
>
> **更新时间：2026-06-29**

---

# 一、数据库概述

## 1.1 数据库信息

| 项目     | 内容                      |
| ------ | ----------------------- |
| 数据库名称  | cat_life                |
| 字符集    | utf8mb4                 |
| 排序规则   | utf8mb4_general_ci      |
| 存储引擎   | InnoDB                  |
| 主键类型   | BIGINT                  |
| 主键生成策略 | Snowflake（MyBatis Plus） |
| 时间类型   | DATETIME                |

---

## 1.2 命名规范

### 表命名

全部采用：

```text
模块_名称
```

例如：

```text
sys_user
cat_info
cat_daily_record
```

---

### 主键

统一：

```text
id
```

---

### 外键

统一：

```text
user_id

cat_id

business_id
```

---

### 时间字段

统一：

```text
create_time

update_time
```

---

### 逻辑删除

统一：

```text
deleted
```

0：正常

1：删除

---

## 1.3 公共字段

所有业务表统一包含：

| 字段          | 类型       | 说明   |
| ----------- | -------- | ---- |
| id          | BIGINT   | 主键   |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted     | TINYINT  | 逻辑删除 |

---

# 二、数据库表设计

## 2.1 用户表（sys_user）

| 字段              | 类型           | 说明          |
| --------------- | ------------ | ----------- |
| id              | BIGINT       | 主键          |
| username        | VARCHAR(50)  | 用户名（唯一）     |
| password        | VARCHAR(255) | BCrypt密码    |
| nickname        | VARCHAR(50)  | 昵称          |
| avatar          | VARCHAR(255) | 用户头像        |
| email           | VARCHAR(100) | 邮箱          |
| phone           | VARCHAR(20)  | 手机号         |
| status          | TINYINT      | 状态（0禁用 1正常） |
| last_login_time | DATETIME     | 最后登录时间      |
| create_time     | DATETIME     | 创建时间        |
| update_time     | DATETIME     | 更新时间        |
| deleted         | TINYINT      | 逻辑删除        |

索引：

* UNIQUE(username)
* UNIQUE(email)
* UNIQUE(phone)

---

## 2.2 猫咪信息（cat_info）

| 字段          | 类型           | 说明   |
| ----------- | ------------ | ---- |
| id          | BIGINT       | 主键   |
| user_id     | BIGINT       | 用户ID |
| name        | VARCHAR(50)  | 猫咪名称 |
| gender      | TINYINT      | 性别（前端 male=0 / female=1） |
| birthday    | DATE         | 出生日期（前端字段 birthDate） |
| breed       | VARCHAR(50)  | 品种 |
| color       | VARCHAR(50)  | 毛色（前端字段 furColor） |
| avatar      | VARCHAR(255) | 猫咪头像 |
| sterilized  | TINYINT      | 是否绝育（前端字段 isNeutered） |
| description | VARCHAR(500) | 简介（前端字段 bio） |
| create_time | DATETIME     | 创建时间 |
| update_time | DATETIME     | 更新时间 |
| deleted     | TINYINT      | 逻辑删除 |

索引：

* INDEX(user_id)

---

## 2.3 日常记录（cat_daily_record）

记录猫咪每日生活。前端 5 个 Tab（饮食喝水/排泄/睡眠/玩耍/状态）统一使用此表，通过 `record_type` 区分。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| cat_id | BIGINT | 猫咪 ID |
| record_type | VARCHAR(20) | 记录类型 |
| record_time | DATETIME | 记录时间（精确到分钟） |
| food_name | VARCHAR(100) | 食物名称（meal） |
| weight | DECIMAL(10,2) | 重量(g)（meal） |
| is_finished | TINYINT | 是否吃完 0/1（meal） |
| water_amount | DECIMAL(10,2) | 饮水量(ml)（meal） |
| poop_type | VARCHAR(20) | pee / poop（poop） |
| poop_status | VARCHAR(50) | 排泄状态（poop） |
| start_time | VARCHAR(10) | 开始时间 HH:mm（sleep） |
| end_time | VARCHAR(10) | 结束时间 HH:mm（sleep） |
| location | VARCHAR(100) | 睡觉地点（sleep） |
| toy | VARCHAR(100) | 玩具名称（play） |
| play_duration | INT | 玩耍时长(分钟)（play） |
| play_photos | JSON | 玩耍照片 URL 数组（play） |
| mood | VARCHAR(20) | energetic / lethargic（mood） |
| behavior | VARCHAR(200) | 行为表现（mood） |
| note | VARCHAR(500) | 备注 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

### record_type

| 类型 | 说明 |
|------|------|
| `meal` | 饮食喝水（合并，包含 food_name / weight / is_finished / water_amount） |
| `poop` | 排泄 |
| `sleep` | 睡眠 |
| `play` | 玩耍 |
| `mood` | 状态 |

> 前端"饮食喝水"为一个 Tab，合并记录食物和饮水数据，不拆分独立表。

索引：

* INDEX(cat_id)
* INDEX(record_time)
* INDEX(record_type)

---

## 2.4 日记（cat_diary）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| cat_id | BIGINT | 猫咪 ID |
| title | VARCHAR(100) | 日记标题 |
| content | TEXT | 日记正文 |
| images | JSON | 图片 URL 数组 ["url1","url2"] |
| tags | JSON | 标签字符串数组 ["标签1","标签2"] |
| record_date | DATE | 日记日期 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

> 标签以 JSON 数组直接存储，不拆分关联表，简化实现。

索引：

* INDEX(cat_id)
* INDEX(record_date)

---

## 2.5 文件资源（cat_file）

用于管理上传的图片。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| business_type | VARCHAR(20) | 业务类型：DIARY / RECORD / PLAY / HEALTH / AVATAR |
| business_id | BIGINT | 业务 ID |
| file_name | VARCHAR(255) | 文件名 |
| file_url | VARCHAR(500) | 文件路径 |
| file_type | VARCHAR(20) | IMAGE |
| storage_type | VARCHAR(20) | LOCAL |
| file_size | BIGINT | 文件大小(Byte) |
| create_time | DATETIME | 创建时间 |

---

## 2.6 首页照片（today_photo）

首页轮播展示的照片，由玩耍记录置顶首页时生成。

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| cat_id | BIGINT | 猫咪 ID |
| url | VARCHAR(500) | 图片 URL |
| record_id | BIGINT | 关联的日常记录 ID |
| create_time | DATETIME | 创建时间 |

索引：

* INDEX(cat_id)

---

## 2.7 健康记录（cat_health_record）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| cat_id | BIGINT | 猫咪 ID |
| health_type | VARCHAR(20) | 类型：vaccine / deworm / medical |
| title | VARCHAR(100) | 标题 |
| record_date | DATE | 记录日期 |
| next_date | DATE | 下次日期 |
| hospital | VARCHAR(100) | 医院名称 |
| note | VARCHAR(500) | 备注 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

### health_type

| 类型 | 说明 |
|------|------|
| vaccine | 疫苗 |
| deworm | 驱虫 |
| medical | 医疗 |

索引：

* INDEX(cat_id)

## 2.8 体重记录（cat_weight_record）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| cat_id | BIGINT | 猫咪 ID |
| weight | DECIMAL(5,2) | 体重(kg) |
| record_date | DATE | 日期 |
| note | VARCHAR(500) | 备注 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |
| deleted | TINYINT | 逻辑删除 |

索引：

* INDEX(cat_id)
* INDEX(record_date)
---

# 三、ER关系

```text
sys_user
    │
    └──────< cat_info
                │
        ├───────┼──────────┬──────────┬───────────┐
        │       │          │          │           │
        ▼       ▼          ▼          ▼           ▼
cat_daily_record  cat_diary  cat_health_record  cat_weight_record  today_photo
        │       │
        │       └──────< cat_file
        │
        └──────< cat_file (玩耍照片)
```

---

# 四、Redis缓存设计

Redis 不存储业务数据，仅用于缓存热点数据。

## 4.1 猫咪档案缓存

Key：

```text
cat:{catId}
```

TTL：

```text
3600 秒（1小时）
```

更新策略：

```text
修改猫咪资料 → 更新 MySQL → 删除 Redis 缓存
```

---

## 4.2 首页统计缓存

Key：

```text
today:{catId}
```

缓存内容：

```json
{
    "mealCount": 2,
    "waterAmount": 350,
    "poopCount": 1,
    "sleepHours": 12,
    "playCount": 1
}
```

TTL：

```text
300 秒（5分钟）
```

更新策略：

```text
新增/修改/删除日常记录 → 删除 today:{catId} → 下次访问重新统计
```

---

## 4.3 登录会话（可选）

Key：

```text
login:{userId}
```

TTL：

```text
7 天
```

用途：

* 单设备登录
* Token失效
* 强制退出登录

---

# 五、文件存储设计

项目采用服务器本地目录存储。

```text
upload
├── avatar
├── diary
├── health
└── temp
```

数据库仅保存文件访问路径，例如：

```text
/upload/avatar/1.jpg
```

服务器部署目录：

```text
/opt/cat-life/
├── app.jar
├── upload
│   ├── avatar
│   ├── diary
│   ├── health
│   └── temp
└── logs
```

---

# 六、索引设计

## sys_user

* UNIQUE(username)
* UNIQUE(email)
* UNIQUE(phone)

## cat_info

* INDEX(user_id)

## cat_daily_record

* INDEX(cat_id)
* INDEX(record_type)
* INDEX(record_time)

## cat_diary

* INDEX(cat_id)
* INDEX(record_date)

## cat_health_record

* INDEX(cat_id)
* INDEX(record_time)

## cat_weight_record

* INDEX(cat_id)
* INDEX(record_date)

---

# 七、数据库优化建议

* 使用 MyBatis Plus 自动填充 `create_time`、`update_time`
* 使用逻辑删除（`deleted`）
* 所有查询默认过滤 `deleted = 0`
* 图片、视频仅保存路径，不保存二进制数据
* 首页统计优先读取 Redis，减少数据库压力
* 修改猫咪信息、日常记录后及时删除缓存，保证数据一致性
* 大文件统一存放在服务器 `upload/` 目录，便于 Nginx 静态资源访问
